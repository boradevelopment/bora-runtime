name: Build on push

on:
  push:

jobs:
  build-linux:
    name: Build on Linux
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout Project
        uses: actions/checkout@v4.2.2

      - name: Create Bora Structure
        run: |
          mkdir -p bora/runtime
          cp -r ./* bora/runtime/
          cd bora
          git clone https://github.com/boradevelopment/bora-global.git global

      - name: Build with CMake
        uses: threeal/cmake-action@v2.1.0
        with:
          source-dir: bora/runtime
          build-dir: build/linux

  build-windows:
    name: Build on Windows
    runs-on: windows-latest
    steps:
      - name: Checkout Project
        uses: actions/checkout@v4.2.2

      - name: Create Bora Structure
        shell: bash
        run: |
          mkdir -p bora/runtime
          xcopy /E /I /H .\* bora/runtime
          cd bora
          git clone https://github.com/boradevelopment/bora-global.git global

      - name: Build with CMake
        uses: threeal/cmake-action@v2.1.0
        with:
          source-dir: bora/runtime
          build-dir: build/win32

  build-macos:
    name: Build on macOS
    runs-on: macos-13
    steps:
      - name: Checkout Project
        uses: actions/checkout@v4.2.2

      - name: Create Bora Structure
        run: |
          mkdir -p bora/runtime
          cp -r ./* bora/runtime/
          cd bora
          git clone https://github.com/boradevelopment/bora-global.git global

      - name: Build with CMake
        uses: threeal/cmake-action@v2.1.0
        with:
          source-dir: bora/runtime
          build-dir: build/darwin
